{
  "name": "paks",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "check": "oxlint && oxfmt --write",
    "dev": "turbo dev",
    "build": "turbo build",
    "build:all": "turbo build cargo:build",
    "test:all": "turbo test cargo:test",
    "lint:all": "turbo lint cargo:lint",
    "fmt:all": "turbo fmt cargo:fmt",
    "check:all": "turbo check cargo:check",
    "check-types": "turbo check-types",
    "generate:schema": "turbo generate:schema",
    "dev:native": "turbo -F native dev",
    "dev:web": "turbo -F web dev"
  },
  "dependencies": {
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "turbo": "^2.6.3",
    "oxlint": "^1.34.0",
    "oxfmt": "^0.19.0",
    "typescript": "^5"
  },
  "packageManager": "pnpm@10.17.1"
}
