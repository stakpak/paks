---
title: Agent Configuration
description: Configure target AI agents for skill installation
---

Paks supports multiple AI coding agents out of the box. Each agent has designated skills directories for both global and project-level installation.

## Built-in Agents

| Agent | Global Directory | Project Directory | Description |
| ----- | ---------------- | ----------------- | ----------- |
| `stakpak` | `~/.stakpak/skills` | `.stakpak/skills` | Stakpak AI agent |
| `claude-code` | `~/.claude/skills` | `.claude/skills` | Anthropic Claude Code |
| `cursor` | `~/.cursor/skills` | `.cursor/skills` | Cursor IDE |
| `codex` | `~/.codex/skills` | `.codex/skills` | OpenAI Codex |
| `copilot` | `~/.copilot/skills` | `.github/skills` | GitHub Copilot |
| `opencode` | `~/.config/opencode/skill` | `.opencode/skill` | OpenCode agent |
| `amp` | `~/.config/agents/skills` | `.agents/skills` | Sourcegraph Amp |
| `goose` | `~/.config/goose/skills` | `.goose/skills` | Block's Goose agent |
| `gemini` | `~/.gemini/skills` | `.gemini/skills` | Google Gemini CLI |
| `windsurf` | `~/.codeium/windsurf/skills` | `.windsurf/skills` | Codeium Windsurf |
| `roo` | `~/.roo/skills` | `.roo/skills` | Roo Code |
| `kilocode` | `~/.kilocode/skills` | `.kilocode/skills` | Kilo Code |
| `antigravity` | `~/.gemini/antigravity/skills` | `.agent/skills` | Google Antigravity |
| `droid` | `~/.factory/skills` | `.factory/skills` | Droid |
| `clawdbot` | `~/.clawdbot/skills` | `skills` | Clawdbot |
| `vscode` | `~/.vscode/skills` | `.vscode/skills` | VS Code with AI extensions |

## Managing Agents

### List All Agents

```bash
paks agent list
```

Shows all configured agents (built-in and custom) with their skills directories.

### Add Custom Agent

```bash
paks agent add my-agent --dir ~/.my-agent/skills
```

Register a custom agent with a specific skills directory.

### Remove Custom Agent

```bash
paks agent remove my-agent
```

Only custom agents can be removed. Built-in agents are permanent.

### Set Default Agent

```bash
paks agent default claude-code
```

Set which agent is used when `--agent` flag is not specified.

### Show Agent Details

```bash
paks agent show stakpak
```

Display configuration details for a specific agent.

## Configuration File

Agent configurations are stored in `~/.paks/config.toml`:

```toml
default_agent = "stakpak"

[agents.my-custom-agent]
skills_dir = "~/.my-custom-agent/skills"
```

## Default Behavior

- When no `--agent` flag is provided, the default agent is used
- Default agent is `stakpak` unless changed with `paks agent default`
- If no agent is configured, skills install to `~/.paks/skills`
